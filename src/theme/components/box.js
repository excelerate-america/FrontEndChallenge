const box = {
  variants: {
    card: (props) => ({
      boxSizing: 'border-sizing',
      boxShadow: '0px 1px 4px rgba(0, 0, 0, 0.1)',
      borderRadius: 2,
      bg: 'white'
    })
  }
}

export default box
